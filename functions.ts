// function pow(x,y){
//   return Math.pow(x,y)
// }
// pow(2,'x')

function pow(x: number, y: number) {
  return Math.pow(x, y);
}
pow(2, 2);

// function pow2(x: number, y:number): string{
//   return Math.pow(x,y)
// }
// pow2(2,2)

function pow2(x: number, y: number): string {
  return Math.pow(x, y).toString()
}
pow2(2, 2);
